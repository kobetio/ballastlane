/**
 * Production environment configuration. Replaced by `environment.development.ts`
 * during `ng serve`/`ng build --configuration development` via the `fileReplacements`
 * entry in `angular.json`.
 */
export const environment = {
  production: true,
  apiBaseUrl: '/api'
};
